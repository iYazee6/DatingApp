// After generating the model we use the help of visual studio code to move it to a new file.
// CTRL + . => Then move to a new file 
// Then updated the name to start with a small letter



export interface Photo {
    id: number;
    url: string;
    isMain: boolean;
    publicId?: any;
}
